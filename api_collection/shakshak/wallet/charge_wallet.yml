info:
  name: charge_wallet
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/user/charge_wallet?value=110.0"
  params:
    - name: value
      value: "110.0"
      type: query
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
