info:
  name: withdraw_request
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/user/withdraw_request?value=110.0"
  params:
    - name: value
      value: "110.0"
      type: query
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
