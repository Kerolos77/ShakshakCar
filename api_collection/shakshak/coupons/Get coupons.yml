info:
  name: Get Available Coupons
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/coupons"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
