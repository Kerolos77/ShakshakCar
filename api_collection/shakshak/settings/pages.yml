info:
  name: pages
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/pages?id=3&locale=ar"
  params:
    - name: id
      value: "3"
      type: query
      description: 1 =>privacy   2=>about 3=>terms
    - name: locale
      value: ar
      type: query
      description: en
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
