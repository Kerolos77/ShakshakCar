info:
  name: notifications mark all as read
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/notifications/read-all"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
