info:
  name: get all notifications
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/notifications"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
