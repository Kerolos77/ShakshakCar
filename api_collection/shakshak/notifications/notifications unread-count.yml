info:
  name: notifications unread-count
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/notifications/unread-count"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
