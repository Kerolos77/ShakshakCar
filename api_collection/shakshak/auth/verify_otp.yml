info:
  name: verify_otp
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/auth/verify_otp?code=111111"
  params:
    - name: code
      value: "111111"
      type: query
  body:
    type: multipart-form
    data:
      - name: code
        type: text
        value: "111111"
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
