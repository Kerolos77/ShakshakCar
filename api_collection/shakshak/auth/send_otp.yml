info:
  name: send_otp
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/auth/send_otp"
  body:
    type: multipart-form
    data:
      - name: phone
        type: text
        value: "{{phone}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
