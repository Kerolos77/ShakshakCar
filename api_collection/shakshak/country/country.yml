info:
  name: country
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/user/country"
  body:
    type: multipart-form
    data:
      - name: phone
        type: text
        value: "01099076093"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
