info:
  name: update favorite-locations
  type: http
  seq: 3

http:
  method: PUT
  url: "{{url}}/user/favorite-locations/2"
  body:
    type: text
    data: "{\r

      \    \"label\": \"الشغل الجديد\",\r

      \    \"is_default\": true\r

      }\r\n"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
