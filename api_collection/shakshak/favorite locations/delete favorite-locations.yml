info:
  name: delete favorite-locations
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{url}}/user/favorite-locations/2"
  body:
    type: text
    data: ""
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
