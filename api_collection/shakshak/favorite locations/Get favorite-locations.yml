info:
  name: Get favorite-locations
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/user/favorite-locations"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
