info:
  name: suggested-places
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/order/suggested-places"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
