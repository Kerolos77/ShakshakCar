info:
  name: send-chat
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/send/chat?trip_id=552&message=adfkn"
  params:
    - name: trip_id
      value: "552"
      type: query
    - name: message
      value: adfkn
      type: query
  body:
    type: multipart-form
    data:
      - name: trip_id
        type: text
        value: "1"
        disabled: true
      - name: message
        type: text
        value: hi
        disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
