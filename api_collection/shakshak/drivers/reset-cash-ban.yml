info:
  name: reset-cash-ban
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/admin/drivers/1/reset-cash-ban"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
