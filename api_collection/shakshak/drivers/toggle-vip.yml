info:
  name: toggle-vip
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/admin/drivers/1/toggle-vip"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
