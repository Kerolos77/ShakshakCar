info:
  name: add-wallet
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/admin/drivers/1/add-wallet"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"
  body:
    type: json
    json: |-
      {
        "amount": 100,
        "notes": "إضافة رصيد محفظة يدوي"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
