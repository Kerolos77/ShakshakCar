info:
  name: get_withdraw_request
  type: http
  seq: 10

http:
  method: GET
  url: "{{url}}/api/v1/user/get_withdraw_request"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
