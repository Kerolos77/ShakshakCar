info:
  name: Delete Card
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{url}}/paymob/saved-cards/2"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
