info:
  name: resolve-payment
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/order/949/resolve-payment"
  body:
    type: multipart-form
    data:
      - name: payment_type
        type: text
        value: cash
      - name: saved_card_id
        type: text
        value: "7"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
