info:
  name: delete-review
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{url}}/admin/reviews/1"
  auth:
    type: bearer
    token: "{{tokenAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
