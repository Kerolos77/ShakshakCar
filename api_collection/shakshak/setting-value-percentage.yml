info:
  name: setting-value-percentage
  type: http
  seq: 23

http:
  method: GET
  url: "{{url}}/settings/percentage-increase"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
