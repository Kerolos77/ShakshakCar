info:
  name: 4. User Denies Counter Offer
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/order/offer/{{offer_id}}/deny"
  headers:
    - name: Accept
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: User denies the driver's counter-offer. Takes offer_id in URL.
