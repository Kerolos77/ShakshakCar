info:
  name: packages status
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/packages/status"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
