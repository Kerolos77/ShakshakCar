info:
  name: Get packages
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/packages"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
