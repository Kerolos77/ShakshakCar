info:
  name: get-user-active-ride
  type: http
  seq: 21

http:
  method: GET
  url: "{{url}}/order/get-user-active-ride"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
