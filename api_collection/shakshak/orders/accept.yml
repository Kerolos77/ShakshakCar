info:
  name: accept
  type: http
  seq: 9

http:
  method: GET
  url: "{{url}}/order/accept/710?driver_id=47"
  params:
    - name: driver_id
      value: "47"
      type: query
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
