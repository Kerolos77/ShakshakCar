info:
  name: end
  type: http
  seq: 12

http:
  method: GET
  url: "{{url}}/order/end/923"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
