info:
  name: current-orders-driver
  type: http
  seq: 8

http:
  method: GET
  url: "{{url}}/order/current-orders-driver"
  params:
    - name: driver_id
      value: "1"
      type: query
      disabled: true
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
