info:
  name: cancel
  type: http
  seq: 13

http:
  method: GET
  url: "{{url}}/order/cancel/967"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
