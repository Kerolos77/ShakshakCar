info:
  name: show order
  type: http
  seq: 20

http:
  method: GET
  url: "{{url}}/order/show/921"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
