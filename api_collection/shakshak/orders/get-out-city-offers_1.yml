info:
  name: get-out-city-offers_1
  type: http
  seq: 6

http:
  method: GET
  url: "{{url}}/order/get-out-city-offers/710"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
