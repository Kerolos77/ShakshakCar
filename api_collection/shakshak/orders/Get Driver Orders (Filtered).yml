info:
  name: Get Driver Orders (Filtered)
  type: http
  seq: 18

http:
  method: GET
  url: "{{url}}/api/v1/order/old-for-driver"
  headers:
    - name: Accept
      value: application/json
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
