info:
  name: start
  type: http
  seq: 11

http:
  method: GET
  url: "{{url}}/order/start/925"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
