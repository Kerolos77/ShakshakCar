info:
  name: driver arrived
  type: http
  seq: 19

http:
  method: GET
  url: "{{url}}/order/arrived/1010"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
