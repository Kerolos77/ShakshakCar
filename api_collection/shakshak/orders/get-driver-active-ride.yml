info:
  name: get-driver-active-ride
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/order/get-driver-active-ride"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
