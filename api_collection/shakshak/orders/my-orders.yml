info:
  name: my-orders
  type: http
  seq: 15

http:
  method: GET
  url: "{{url}}/order/my"
  body:
    type: multipart-form
    data:
      - name: service_id
        type: text
        value: "4"
      - name: destination
        type: text
        value: Cairo, Egypt
      - name: origin
        type: text
        value: Cairo, Egypt
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
