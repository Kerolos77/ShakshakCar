info:
  name: settings
  type: http
  seq: 22

http:
  method: GET
  url: "{{url}}/settings"
  auth:
    type: bearer
    token: "{{tokenUser}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
