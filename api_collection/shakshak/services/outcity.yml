info:
  name: outcity
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/services/outcity"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
