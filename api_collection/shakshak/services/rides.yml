info:
  name: rides
  type: http
  seq: 4

http:
  method: GET
  url: "{{url}}/services/rides"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
