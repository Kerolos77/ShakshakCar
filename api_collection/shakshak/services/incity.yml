info:
  name: incity
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/services/incity"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
