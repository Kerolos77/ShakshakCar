info:
  name: travels
  type: http
  seq: 5

http:
  method: GET
  url: "{{url}}/services/travels"
  auth:
    type: bearer
    token: "{{tokenDriver}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
