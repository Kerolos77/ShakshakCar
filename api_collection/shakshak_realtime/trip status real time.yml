info:
  name: trip status real time
  type: websocket
  seq: 1

websocket:
  url: "{{real_time_url}}"
  message:
    type: json
    data: '{"event":"pusher:subscribe","data":{"channel":"trip-970"}}'
  auth: inherit

settings:
  timeout: 0
  keepAliveInterval: 0
